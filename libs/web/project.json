{
  "name": "web-lib",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/web/src",
  "projectType": "library",
  "tags": ["scope:libs", "type:lib", "platform:web"],
  "targets": {
    "build": {
      "command": "pnpm build",
      "options": { "cwd": "libs/web" },
      "outputs": ["{projectRoot}/dist"]
    },
    "format": {
      "command": "pnpm format",
      "options": { "cwd": "libs/web" }
    },
    "format-check": {
      "command": "pnpm format-check",
      "options": { "cwd": "libs/web" }
    },
    "lint": {
      "command": "pnpm lint",
      "options": { "cwd": "libs/web" }
    },
    "lint-check": {
      "command": "pnpm lint-check",
      "options": { "cwd": "libs/web" }
    },
    "type-check": {
      "command": "pnpm type-check",
      "options": { "cwd": "libs/web" }
    },
    "knip": {
      "command": "knip --config configs/knip/knip.json --workspace libs/web",
      "options": { "cwd": "." }
    },
    "validate": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "nx run {projectName}:format",
          "nx run {projectName}:lint",
          "nx run {projectName}:type-check",
          "nx run {projectName}:knip"
        ],
        "parallel": false
      }
    }
  }
}
