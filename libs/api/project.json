{
  "name": "api-lib",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/api/src",
  "projectType": "library",
  "tags": ["scope:libs", "type:lib", "platform:node"],
  "targets": {
    "build": {
      "command": "pnpm build",
      "dependsOn": ["^build"],
      "options": {
        "cwd": "libs/api"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "format": {
      "command": "pnpm format",
      "options": {
        "cwd": "libs/api"
      }
    },
    "format-check": {
      "command": "pnpm format-check",
      "options": {
        "cwd": "libs/api"
      }
    },
    "lint": {
      "command": "pnpm lint",
      "dependsOn": ["^build"],
      "options": {
        "cwd": "libs/api"
      }
    },
    "lint-check": {
      "command": "pnpm lint-check",
      "dependsOn": ["^build"],
      "options": {
        "cwd": "libs/api"
      }
    },
    "type-check": {
      "command": "pnpm type-check",
      "dependsOn": ["^build"],
      "options": {
        "cwd": "libs/api"
      }
    },
    "test": {
      "command": "pnpm test",
      "dependsOn": ["^build"],
      "options": {
        "cwd": "libs/api"
      }
    },
    "knip": {},
    "validate": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": ".",
        "commands": [
          "nx run api-lib:format",
          "nx run api-lib:lint",
          "nx run api-lib:type-check",
          "nx run api-lib:test",
          "nx run api-lib:knip"
        ],
        "parallel": false
      }
    }
  }
}
