{
  "name": "locale",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/locale/src",
  "projectType": "library",
  "tags": ["scope:libs", "type:lib"],
  "targets": {
    "build": {
      "command": "pnpm build",
      "options": {
        "cwd": "libs/locale"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "format": {
      "command": "pnpm format",
      "options": {
        "cwd": "libs/locale"
      }
    },
    "format-check": {
      "command": "pnpm format-check",
      "options": {
        "cwd": "libs/locale"
      }
    },
    "lint": {
      "command": "pnpm lint",
      "options": {
        "cwd": "libs/locale"
      }
    },
    "lint-check": {
      "command": "pnpm lint-check",
      "options": {
        "cwd": "libs/locale"
      }
    },
    "type-check": {
      "command": "pnpm type-check",
      "options": {
        "cwd": "libs/locale"
      }
    },
    "knip": {},
    "validate": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": ".",
        "commands": [
          "nx run locale:format",
          "nx run locale:lint",
          "nx run locale:type-check",
          "nx run locale:knip"
        ],
        "parallel": false
      }
    }
  }
}
