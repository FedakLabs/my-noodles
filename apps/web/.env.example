NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional — analytics off when unset (GA4/Hotjar live in GTM, not as Next env vars)
# Set + restart next to test locally; then /uk/dev/analytics + GTM Preview / GA4 DebugView
# NEXT_PUBLIC_GTM_ID=

# Optional — browser error reporting off when unset
# NEXT_PUBLIC_SENTRY_DSN=https://<key>@o<org>.ingest.sentry.io/<project>
# NEXT_PUBLIC_APP_VERSION=dev
