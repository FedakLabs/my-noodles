# Server origin — generated client paths include `/api/...`
NEXT_PUBLIC_API_URL=http://localhost:3001

# Public storefront origin — canonical URLs, sitemap, Open Graph
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Analytics (Phase 9 — fill before launch)
# NEXT_PUBLIC_GTM_ID=
# NEXT_PUBLIC_GA4_ID=
