/** Global Nest route prefix — must stay in sync with `index.ts`. */
export const API_GLOBAL_PREFIX = 'api';

/** Swagger UI — served at `/${SWAGGER_UI_PATH}` (outside the API global prefix). */
export const SWAGGER_UI_PATH = 'docs';

/** OpenAPI JSON — served at `/${SWAGGER_JSON_PATH}`. */
export const SWAGGER_JSON_PATH = 'docs-json';
