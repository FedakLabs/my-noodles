/** Global Nest route prefix — must stay in sync with `index.ts`. */
export const API_GLOBAL_PREFIX = 'api';

/** Swagger UI — served at `/${SWAGGER_UI_PATH}`. */
export const SWAGGER_UI_PATH = 'api/docs';

/** OpenAPI JSON — served at `/${SWAGGER_JSON_PATH}` (mvp-plan: `/api/docs-json`). */
export const SWAGGER_JSON_PATH = 'api/docs-json';
