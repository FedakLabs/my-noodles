# CI pipeline (staged for clarity, parallel where safe):
#
#   Stage 1 — quality (parallel): format-check, lint-check, type-check, knip
#   Stage 2 — unit tests:          nx affected -t test
#   Stage 3 — e2e:                 nx affected -t e2e (web funnel; skipped when web untouched)
#
# Local parity:
#   pre-commit  → lint-staged + nx affected -t type-check,knip --uncommitted
#                 (--uncommitted: hook runs before the commit exists)
#   full gate   → CI workflow (atomic read-only jobs; base…HEAD already on the branch)
#
# CI calls atomic Nx targets (not the composite `validate`) so each job
# maps to one concern in the GitHub UI and failures are easy to spot.

name: CI

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  actions: read

jobs:
  # ── Stage 1: quality (parallel) ───────────────────────────────────────────

  format-check:
    name: Quality · Format
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: ./.github/actions/setup-monorepo
      - name: oxfmt (affected)
        run: pnpm nx affected -t format-check --parallel=3

  lint-check:
    name: Quality · Lint
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: ./.github/actions/setup-monorepo
      - name: oxlint (affected)
        run: pnpm nx affected -t lint-check --parallel=3

  type-check:
    name: Quality · Types
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: ./.github/actions/setup-monorepo
      - name: TypeScript (affected)
        run: pnpm nx affected -t type-check --parallel=3

  knip:
    name: Quality · Knip
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: ./.github/actions/setup-monorepo
      - name: Unused exports/deps (affected)
        run: pnpm nx affected -t knip --parallel=3

  # ── Stage 2: unit tests ─────────────────────────────────────────────────────

  unit-test:
    name: Test · Unit
    runs-on: ubuntu-latest
    needs: [format-check, lint-check, type-check, knip]
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: ./.github/actions/setup-monorepo
      - name: Jest / Vitest (affected)
        run: pnpm nx affected -t test --parallel=3

  # ── Stage 3: e2e ──────────────────────────────────────────────────────────

  e2e:
    name: Test · E2E
    runs-on: ubuntu-latest
    needs: [unit-test]
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: ./.github/actions/setup-monorepo
      - name: Cache Playwright browsers
        uses: actions/cache@v5
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}
      - name: Install Playwright Chromium
        run: pnpm exec playwright install chromium --with-deps
      - name: Playwright funnel (affected)
        run: pnpm nx affected -t e2e --parallel=1
        env:
          CI: true

  infrastructure:
    name: Quality · Infrastructure
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      DEPLOY_ENVIRONMENT: ${{ vars.CI_DEPLOY_ENVIRONMENT || 'prod' }}
    steps:
      - uses: actions/checkout@v5
      - uses: ./.github/actions/setup-monorepo
      - uses: opentofu/setup-opentofu@v1
        with:
          tofu_version: 1.10.0
      - name: OpenTofu format check
        run: tofu fmt -check -recursive devops/tofu
      - name: OpenTofu validate
        env:
          TF_VAR_state_passphrase: local-validation-only-change-me
        run: pnpm tofu:validate
      - name: Deployment environment checks
        run: |
          pnpm --dir devops/cloudflare run validate:environments
          for environment_path in devops/envs/*; do
            environment="${environment_path##*/}"
            node .github/scripts/validate-deployment-environment.mjs "$environment" false
            for application in web admin api; do
              GITHUB_ENV="$RUNNER_TEMP/$environment-$application.env" \
                node .github/scripts/load-deployment-environment.mjs "$environment" "$application"
            done
          done
          for application in web admin api; do
            node .github/scripts/load-deployment-environment.mjs "$DEPLOY_ENVIRONMENT" "$application"
          done
      - name: Build OpenNext deployment bundle
        run: pnpm --dir devops/cloudflare run web:build
      - name: Build admin deployment assets
        run: pnpm nx run admin:build
      - name: Wrangler configuration checks
        run: |
          pnpm --dir devops/cloudflare run web:check
          pnpm --dir devops/cloudflare run admin:deploy -- --dry-run
          pnpm --dir devops/cloudflare run api:deploy -- --dry-run
