name: Set deployment metadata
description: Resolve release metadata from the commit that was actually checked out

runs:
  using: composite
  steps:
    - name: Export checked-out commit
      shell: bash
      run: printf 'DEPLOY_SHA=%s\n' "$(git rev-parse HEAD)" >> "$GITHUB_ENV"
