export const DISCOVERY_CARD_IMAGE_FRAME_SX = {
  aspectRatio: '1',
  borderRadius: 1.5,
  overflow: 'hidden',
  bgcolor: 'action.hover',
  flexShrink: 0,
} as const;

/** Theme button minHeight (44) + DiscoveryCardActions 1px border top/bottom. */
export const DISCOVERY_CARD_ACTIONS_ROW_MIN_HEIGHT = 46;
